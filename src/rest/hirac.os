#Использовать "./core/modules"

Функция ОбработкаВызоваHTTPСервиса(Запрос) Экспорт

	Если Запрос.HTTPМетод = "GET" Тогда
		
		Результат = ОбработкаКоманд.ВызватьУтилитуАдминистрирования(Новый Соответствие(Запрос.ПараметрыЗапроса));

		Ответ = Новый HTTPСервисОтвет(200);
		Ответ.УстановитьТелоИзСтроки(Результат);
		Возврат Ответ;
	Иначе
		Возврат Новый HTTPСервисОтвет(403);
	КонецЕсли;
		
КонецФункции